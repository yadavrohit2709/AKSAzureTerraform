# Test Workflow - Simple Hello World

name: Test GitHub Actions

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Hello World
        run: |
          echo "Hello from GitHub Actions!"
          echo "If you see this, GitHub Actions is working!"
